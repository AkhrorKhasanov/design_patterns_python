Что такое Facade (Фасад)?

**Фасад** — это структурный паттерн проектирования, который предоставляет **упрощённый интерфейс** к сложной системе.
Вместо того чтобы работать напрямую с множеством классов и их методами, клиент получает **один простой объект (Фасад)**, который внутри вызывает все нужные компоненты.

Пример из жизни:

* Когда вы нажимаете кнопку **"Power"** на компьютере, внутри запускаются десятки сложных процессов (питание, загрузка BIOS, чтение с диска, запуск ОС).
* Но пользователю это не нужно знать — он просто нажал одну кнопку.
  👉 Кнопка **Power** = **Фасад**.

---

Где применяется Facade?

* **Фреймворки** (например, Laravel, Django) — ORM, роутинг, шаблоны, сессии работают внутри, а разработчик работает через простой API.
* **Библиотеки** — например, `matplotlib.pyplot.plot()` внутри запускает сложные механизмы отрисовки, но вы вызываете только одну функцию.
* **ОС и драйверы** — пользователю доступна простая кнопка или команда, но внутри сложная логика.
